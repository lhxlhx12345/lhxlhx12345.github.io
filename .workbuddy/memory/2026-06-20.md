- 创建了 `GITHUB_CLI_PUSH_GUIDE.md`，提供使用 GitHub CLI（gh）进行文件提交、推送以及创建 Pull Request 的完整步骤。
- 创建了 `push_changes.sh` 脚本，封装了 `git add -A && git commit -m "msg" && git push`，并在推送前检查 gh 是否已登录。
- 记录上述操作以便后续参考。
- 创建了 `auth_github_cli.sh` 脚本，提供通过交互式或 PAT 方式完成 GitHub CLI 认证的示例。